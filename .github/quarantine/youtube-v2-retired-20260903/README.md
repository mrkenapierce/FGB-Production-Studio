# Retired YouTube v2 Actions

These workflows belonged to the retired YouTube v2 media path. They are stored outside `.github/workflows`, so GitHub Actions cannot execute them. Do not reactivate them. The authorized deployment and validation workflows are `youtube-v3-deploy.yml` and `youtube-v3-validate.yml`.
