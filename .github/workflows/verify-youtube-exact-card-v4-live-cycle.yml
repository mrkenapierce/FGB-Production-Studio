name: Legacy Exact-Box Trivia Cycle Verifier - Quarantined

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quarantined:
    runs-on: ubuntu-latest
    steps:
      - name: Retired architecture guard
        run: |
          echo 'QUARANTINED: this workflow targeted the retired Lovable routing/compositor architecture.'
          echo 'Production now uses only fgbears-youtube-output.service.'
          echo 'No SSH, deployment, restart, certification write-back, or legacy service action is permitted here.'
