name: Legacy Full-Middle Concealment - Manual Only

on:
  workflow_dispatch:

permissions:
  contents: read

# Legacy emergency reference only. This workflow is intentionally manual-only.
# Production now uses the resource-protected 640x360 exact-question-box path:
# source 1280x720 region x=462,y=104,w=798,h=470 maps to
# YouTube x=231,y=52,w=399,h=235. Do not restore the full-middle geometry.

jobs:
  legacy_notice:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete automatic deployment
        shell: bash
        run: |
          echo 'This legacy full-middle deployment is disabled.'
          echo 'Use the locked exact-box low-lag YouTube production path instead.'
          exit 1
