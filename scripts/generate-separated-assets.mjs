// Existing GitHub workflow entry point.
// This now delegates to the production package renderer so GitHub Actions can render
// the Episode 024 production screen package without requiring a workflow-file edit.
import './render-production-package.mjs';
